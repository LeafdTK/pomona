# Copy to .env and fill in. Anything already in the real environment wins, so a
# deployment can override this file without editing it.

# api.slack.com/apps -> your app -> Basic Information -> App Credentials
POMONA_SLACK_CLIENT_ID=
POMONA_SLACK_CLIENT_SECRET=
